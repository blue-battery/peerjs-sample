version: '3'
services:
  peerjs:
    container_name: peerjs
    tty: true
    build:
      context: ./
      dockerfile: ./docker/peerjs/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./peerjs:/peerjs/
    working_dir: /peerjs/
    privileged: true
    healthcheck:
      test: "exit 0"
